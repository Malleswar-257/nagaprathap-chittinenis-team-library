model User {
  id       String   @id @default(cuid())
  email    String   @unique
  password String
}

model Asset {
  id       String   @id @default(cuid())
  name     String
  user     User     @relation(fields: [id], references: [id])
}